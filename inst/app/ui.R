### Load libraries ###
library(shiny)
library(shinyAce)
library(shinyFiles)
library(shinyBS)
### ###


shinyUI(
  bootstrapPage(
    title = "editR",
    
    header, 
    
    hidden,
    
    navbar,
    
    editor,
    
    preview
  )
)










